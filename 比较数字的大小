#include<stdio.h>
int main(){
	double m = 0;
	double n = 0;
	printf("请输入两个数\n");
	scanf("%lf%lf", &m, &n);
	if(m < n)
			printf("较大的数是%lf\n", n);
	else if(m = n)
			printf("两数相同\n");
	else
			printf("较大的数是%lf\n", m);
	return 0;
} 
